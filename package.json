{
  "name": "digitale-gewinner-unified",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "payload-cms"
  ],
  "scripts": {
    "build:site": "bash build.sh && rm -rf payload-cms/public && mkdir -p payload-cms/public && cp -R dist/. payload-cms/public/",
    "build": "npm run build:site && npm --workspace payload-cms run build",
    "dev": "npm run build:site && npm --workspace payload-cms run dev",
    "start": "npm --workspace payload-cms run start"
  },
  "engines": {
    "node": ">=20.9.0"
  }
}
